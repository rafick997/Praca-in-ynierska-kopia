@model IEnumerable<Przychodnia_medyczna.Models.TestType>

@{
    ViewBag.Title = "Index";
    Layout = "~/Views/Shared/_AdminLayout.cshtml";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.PatientTest.TestName)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Name)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.PatientTest.TestName)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Name)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.TestTypeId }) |
            @Html.ActionLink("Details", "Details", new { id=item.TestTypeId }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.TestTypeId })
        </td>
    </tr>
}

</table>
